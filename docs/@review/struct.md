# Notes on the fundamental structures of ^m, and future plans.
The fundamental structure of ^meta is essentially pushing programmable executable grammar, for cognitive ergonomics. 


## Impelmentable versions
We also think in terms of implementation someday. Perhaps, metalang itself is a system of reasoning standards of computational systems, rather an a language itself.

A cognitive, functional framework, with common grammar and patterns, with the ability to implement across different functional programs (C, java, javascript, ruby, etc.) All sharing the same fundamental ideas. 

This might make it incredibly difficult due to differences in environments and run times, but at the very least it explores metalang not as a framework, but research on cognitive human ergonomics, which so happens to also exist as a completely functional prototype language. shaping the way we think about our computer programs, but also being executable itself. it's sort of paradoxical, much like code itself: (we've taught rocks to think and do math!)

This explains the most common structure for which methylene is ordered. basically, we use objects and strings. because introducing new syntax files, gives us the infinite expressibility of plain text possibilities, but it also will probably lead to the same problems as modern programming anyway. this also allows us to keep the language flexible, just in case someday we really do want to create implementations across other languages. in that sense it would definitely be very meta! 

```js
new Variable("String based string?")
    .dtring("but what is a string?")
    .vbr("a string is a vibration, as explored in string theory! here we have a fundamental visualization to explain this phenomenon.")
    .vis("spaghetti")